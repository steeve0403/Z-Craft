import React from 'react'
import {BrowserRouter as Router} from 'react-router-dom'

const App: React.FC = () => {
    return (
        <Router>
            <div className="layout">

            </div>
        </Router>
    );
};

export default App
